
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <%= render 'user_info' %>
      <%= render 'follow_form' if logged_in? %>
    </section>
  </aside>
  <div class="col-md-8">
  <h3>フォロー</h3>
  <ul class="list-group">
    <% @followings_user.each do |followings_user| %>
      <li class="list-group-item">
        <h4><%= link_to followings_user.name, followings_user %></h4>
        <%= followings_user.profile %>
      </li>
    <% end %>
  </ul>
  </div>
</div>
